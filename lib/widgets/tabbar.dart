import 'package:flutter/material.dart';

class ManualWidgetTesterTabbar extends StatelessWidget {
  const ManualWidgetTesterTabbar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}